{% extends 'base.html.twig' %}

{% block title %}
    {{ groupeTesteurs.groupTesteurLabel }}
{% endblock %}

{% block body %}
    <h1>{{ groupeTesteurs.groupTesteurLabel }}</h1>

    <!-- Image Section -->
    <div>
        <img src="{{ asset('path/to/your/image.jpg') }}" alt="{{ groupeTesteurs.groupTesteurLabel }} Image">
    </div>

    <!-- Details Section -->
    <div>
        <dl>
            <dt>ID:</dt>
            <dd>{{ groupeTesteurs.id }}</dd>

            <dt>Description:</dt>
            <dd>{{ groupeTesteurs.groupTesteurDescription }}</dd>

            <dt>Created At:</dt>
            <dd>{{ groupeTesteurs.createdAt|date('Y-m-d H:i:s') }}</dd>
        </dl>
    </div>

    <!-- Enseignants Table Section -->
    <h2>Enseignants</h2>
    <table id="enseignants-table">
        <thead>
            <tr>
                <th>ID</th>
                <th>First Name</th>
                <th>Last Name</th>
                <!-- Add more columns as needed -->
            </tr>
        </thead>
        <tbody>
            {% for enseignant in enseignants %}
                <tr>
                    <td>{{ enseignant.id }}</td>
                    <td>{{ enseignant.firstName }}</td>
                    <td>{{ enseignant.lastName }}</td>
                    <!-- Add more columns as needed -->
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <!-- Include DataTables JS and CSS -->
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/v/dt/dt-1.10.25/datatables.min.css"/>
    <script type="text/javascript" src="https://cdn.datatables.net/v/dt/dt-1.10.25/datatables.min.js"></script>

    <!-- Initialize DataTable -->
    <script>
        $(document).ready(function() {
            $('#enseignants-table').DataTable();
        });
    </script>
{% endblock %}
